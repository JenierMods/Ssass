# packages/ui

Librería de componentes visuales compartidos para React.

Incluye botones, inputs, layouts, tablas, modales y patrones de diseño reutilizables. Mantener una UI compartida ayuda a escalar el producto sin duplicar componentes en cada pantalla.
